import { PageMeta } from "@/public-site/PageIntro";
import { useEffect, useState } from "react"; import { UpdateCard } from "@/components/UpdateCard"; import { fetchUpdates, type Update } from "@/lib/updates";
export default function Updates(){ const [items,setItems]=useState<Update[]|null>(null); useEffect(()=>{fetchUpdates().then(setItems).catch(()=>setItems([]));},[]); return <main className="mx-auto min-h-[60vh] max-w-7xl px-5 py-14 sm:px-8 lg:px-10"><PageMeta title="Updates" description="News, events, releases, research and opportunities from the Sustainable Design Standard." /><p className="text-sm font-extrabold uppercase tracking-wider text-[#28775e]">SD Standard</p><h1 className="mt-3 text-5xl font-extrabold">Updates</h1><p className="mt-5 max-w-3xl text-lg text-[#5f5a50]">News, events, releases, research, and opportunities from the Sustainable Design Standard.</p>{items === null ? <p className="mt-10">Loading updates…</p> : items.length ? <section className="mt-10 grid gap-5 md:grid-cols-2 lg:grid-cols-3" aria-label="Published updates">{items.map((u)=><UpdateCard update={u} key={u.slug}/>)}</section> : <p className="mt-10 rounded-lg border border-[#d9d4c8] bg-white p-6">There are no published updates yet. Please check back soon.</p>}</main>; }
