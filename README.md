# SD Impact Calculator

A static, client-side web app that lets a user rate a design project across 4 pillars: Environmental, Social, Cultural, Financial.

## Running Locally
1. Run `npm install`
2. Run `npm run dev`
3. The app is completely static, loading its configuration from `client/public/data/criteria.json`.